//配置文件（配置main.js所依赖的模块）
require.config({
	paths : {
		"jquery" : "jquery-1.11.3",
		"index" : "index",
		"vari2" : "variblu2"
	}
})
require(["jquery","cookie","vari2"],function($,cookie,vari2){
	//实现代码
	
})
